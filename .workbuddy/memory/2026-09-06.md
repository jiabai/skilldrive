# 2026-09-06

## README 一致性核对与修复
- 核对根目录 README.md / README-zh.md 与代码实际状态，发现 3 处过时内容并全部修复：
  1. 桌面端示例中 `SKILLDRIVE_CODEX/CLAUDE_CODE/GEMINI_CLI_SKILLS_PATH` 三个环境变量已在代码中移除（实际仅 7 个 `SKILLDRIVE_*` 变量生效），改为 `SKILLDRIVE_POLL_INTERVAL_MS` 并注明路径由客户端内配置管理。
  2. 支持的 Agent 表补齐代码中新增的 3 个：WorkBuddy、Hermes Agent、QwenWork CN（共 24 个，源：`desktop-client/src/adapters/agents/definitions.ts`）。
  3. 技能下载端点更正为 `POST /api/v1/client/skills/download`（源：`backend/api/v1/client_skills.py` + `router.py` 前缀 `/client/skills`），共修 4 处（两份 README 的 mermaid 图/接入流程/端点表）。
- 同步修复 `desktop-client/README.md` 与 `README-zh.md` 中相同的过时环境变量说明。
- 顺手修复英文 README 中 Agent 表注释行的乱码字符（U+FFFD → 💡）。
- 未改动（历史文档，保留原貌）：`technical-analysis-docs/project-function-overview.md`、`desktop-client/docs/design-docs/`、`desktop-client/docs/product-specs/2026-04-27-agent-detection-and-distribution.md` 中仍有旧变量/旧端点引用。
- 经验：同一文件的多个 Edit 不要并行调用，会互相覆盖（本次 mermaid 行的编辑被同批后续写入覆盖，需串行重做）。
